apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: sre-project
spec:
  replicas: 1
  selector:
    matchLabels: { app: notification-service }
  template:
    metadata:
      labels: { app: notification-service }
    spec:
      containers:
        - name: notification-service
          image: sre-notification-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 3005 }]
          env:
            - { name: PORT, value: "3005" }
            - { name: SERVICE_NAME, value: notification-service }
          readinessProbe: { httpGet: { path: /health, port: 3005 }, initialDelaySeconds: 5, periodSeconds: 10 }
          livenessProbe: { httpGet: { path: /health, port: 3005 }, initialDelaySeconds: 15, periodSeconds: 20 }
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: sre-project
spec:
  selector: { app: notification-service }
  ports: [{ port: 3005, targetPort: 3005 }]

